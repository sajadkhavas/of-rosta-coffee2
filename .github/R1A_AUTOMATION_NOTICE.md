# Temporary R1A executor

This branch exists only to execute and evidence the bounded R1A dependency-contract repair against `program/r1a-dependency-contract`.

It is not a product branch and must never be merged into `integration/rosta-r-program` or `main`.
